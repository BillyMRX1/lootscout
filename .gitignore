# Local agent / tooling
.claude/
docs/

# Secrets & state
.env
config.toml        # holds ntfy topic + telegram chat_id (obscurity); use config.example.toml
seen.json

# RSS output (regenerated every run)
public/feed.xml

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
build/
dist/
