# Local agent / tooling
.claude/
docs/

# Secrets & state
.env
# config.toml holds your telegram chat_id etc. — keep it local; use config.example.toml
config.toml
seen.json

# RSS output (regenerated every run)
public/feed.xml

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
build/
dist/
